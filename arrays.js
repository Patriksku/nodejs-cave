'use strict';

let days = ['Sunday', 'Monday', 'Tuesday', 1];
let day = days[1];
let numbah = days[3];

console.log(days[0]);
console.log(days);
console.log('size:', days.length);

for (let i = 0; i < days.length; i++) {
  console.log(days[i]);
}